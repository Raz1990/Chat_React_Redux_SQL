/*import ICanChat from "./../../Interfaces/ChatEntity";
import {SET_CONTACTS} from "./../actionTypes";

export default function loadAll() {
    return dispatch => {
        const contacts = [
            {id: 1, name: "Ori"},
            {id: 2, name: "Roni"},
        ];

        dispatch(setAll(contacts));
    }
}

function setAll(contacts: ICanChat[]) {
    return {
        type: SET_CONTACTS,
        contacts,
    }
}*/
